window.addEventListener("load", function () {
    fetch('http://localhost:9000/sastojci/').then(Response => Response.json())
    .then(sastojci => {
        for (let i = 0; i < sastojci.length; i++) {
            if()
        }
    })
    .catch(error => {
        console.error('Error:', error);
    });

    fetch('http://localhost:9000/sastojci/').then(Response => Response.json())
    .then(data => {
        for (let i = 0; i < data.length; i++) {
            
        }
    })
    .catch(error => {
        console.error('Error:', error);
    });

    fetch('http://localhost:9000/sastojci/').then(Response => Response.json())
    .then(data => {
        for (let i = 0; i < data.length; i++) {
            
        }
    })
    .catch(error => {
        console.error('Error:', error);
    });

    fetch('http://localhost:9000/sastojci/').then(Response => Response.json())
    .then(data => {
        for (let i = 0; i < data.length; i++) {
            
        }
    })
    .catch(error => {
        console.error('Error:', error);
    });


});






